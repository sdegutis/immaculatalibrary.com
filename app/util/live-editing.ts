// import { spawnSync } from 'child_process';

// export function pushChanges(path: string, msg: string) {
//   console.log('pushing(): starting...', [path, msg]);
//   spawnSync(`git add "${path}"`, { shell: true, stdio: 'inherit' });
//   spawnSync(`git commit -m "${msg}"`, { shell: true, stdio: 'inherit' });
//   spawnSync('git push', { shell: true, stdio: 'inherit' });
//   console.log('pushing(): done');
// }
